#include<iostream>
using namespace std;

int main()
{
  int A, B, C;

  cin >> A >> B >> C >> D;



  if ( A >= B && A >= C && ) {
    cout << A << " is the largest!" << '\n';
  }
  else if ( B > C && B > A) {
    std::cout << B << " is the largest!" << '\n';
  }
  else {
    std::cout << C << " is the largest!" << '\n';
  }

  return 0;
}
